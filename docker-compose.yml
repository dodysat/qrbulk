version: '3.8'

services:
  qr-generator:
    build: .
    container_name: qr-bulk-generator
    volumes:
      - .:/data
    environment:
      - CSV_FILE=sample.csv
      - OUTPUT_ZIP=/data/output.zip
    restart: "no"

